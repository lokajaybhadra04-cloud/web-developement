IN  backend    : 

database: collection of data to access later.

MYSQL(database), SQL(language)  later we see monagoDB(non-retaionl database)

WE see CURD operations with database  ,Not like array(we made) nor data.json(data actually never store in json files).










db(SQL DB)--> data store in tables   

example: MYSQL,ORACLE,POStgreSQL etc

db(NOSQL DB)--> data store in doc,key-value pair(like json files),graphs 

example:MongoDb,cassandra,NEO4j














SQL:  used interact with relational databases.


Install:

MYSQL server,MYSQL workbench (app)




















