
Libraray vs framwork:

libraray :a libraray is a collection of pre written code that can be used to perfrom specfic tasks.

example:

   axios function



Framework: A framework is a set pre-written code that provides a structure for devloping
            software applications

example: express






EXPRESS:

A node.js web application framework that helps us to make web applications .
it is used for server side programming. until we done client side programming.


used express :

--> listen for incoming for incoming request

--> prase the request

--> match response with routes




express inside "npm"  packages









------------------------------------------------------------------------------------------------------








creating   "WEB-based  API" using express

--> create directory node express

--> now "npm init" we initlzation package.jason

--> install express

--> create file index.js where we write our code.this  "index.js"  will act as " server "




































NODEMON:  to automatically restart server with code chages.


//every time we make litte changes in the index.js file(server) we had to stop server ctrl+c  and start server again  "node index.js" every time.

//so we use nodemon(like express it also package).

->sudo npm install -g nodemon (install goblally)
 ->nodemon index.js






































































 //path parameters: when  we define  "variable"  as the  " path parameters ".

 app.get("/:varaible/:varabile1",handler as call back function)
 












//query strings:

//https://www.google.com/search?q=apple is query string in url for "q=apple"

//to handle query string we create new path
















